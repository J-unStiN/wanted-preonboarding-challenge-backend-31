spring:
  application:
    name: backend
  datasource:
    url: jdbc:postgresql://localhost:5432/backend # ?????? ???? ?????
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: none # Flyway? ????? validate ?? none ??
    properties:
      hibernate:
        format_sql: true # SQL ?? ??? ?? (?? ??)
    show-sql: true # ???? SQL ?? ?? (?? ? ??)
  flyway:
    enabled: true
    baseline-on-migrate: true
    locations: classpath:db/migration,classpath:db/migration/data
    clean-disabled: false